"""
Payment gateway adapters.

The ONLY place a gateway SDK or a gateway-specific identifier may appear. A
test asserts that, because an abstraction maintained by good intentions is not
one.

    fake.py       in-memory. The domain is fully testable with no network.
    razorpay/     NOT BUILT. Phase 4 Step 3.
"""
