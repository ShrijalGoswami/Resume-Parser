/**
 * Marketing motion & WebGL effects.
 *
 * Every export here is opt-in and self-contained: each one degrades to static,
 * fully-visible content under `prefers-reduced-motion: reduce`, and none of
 * them is required for the page to read correctly.
 */
export { ConstellationBackground } from './constellation-background'
export { MaskReveal, BlurReveal, StaggerGroup } from './scroll-reveal'
export { TiltCard } from './tilt-card'
